<Window x:Class="HBSort.Views.BinCreateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Lagerfach anlegen"
        Width="400" Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Label:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="LabelBox" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Notiz (optional):" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="NotesBox"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 Height="80" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="4" x:Name="ErrorText"
                   Foreground="Red" FontSize="11"
                   Margin="0,8,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Abbrechen" IsCancel="True" Width="100" Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Anlegen" IsDefault="True" Width="100"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
