<UserControl x:Class="HBSort.Views.BuildSuggestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        PROMPT 11: Bauvorschlaege auf Basis der Floating-Parts.
        DataContext = BuildSuggestionsViewModel.
    -->
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="WAS KANN ICH BAUEN?"
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,4"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Mind. Match:"
                       VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="Gray" FontSize="11"/>
            <Slider Grid.Column="1" Minimum="10" Maximum="100"
                    TickFrequency="10" IsSnapToTickEnabled="True"
                    Value="{Binding MinMatchPercent}"
                    VerticalAlignment="Center"/>
            <TextBlock Grid.Column="2" Margin="8,0,0,0"
                       VerticalAlignment="Center" FontWeight="SemiBold"
                       Text="{Binding MinMatchPercent, StringFormat={}{0}%}"/>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Suggestions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="6" Margin="0,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding ImageUrl}"
                                       Width="44" Height="44" Stretch="Uniform"/>
                                <StackPanel Grid.Column="1" Margin="6,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                               FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock FontSize="10" Foreground="Gray">
                                        <Run Text="BL:"/>
                                        <Run Text="{Binding BricklinkId, Mode=OneWay}" FontFamily="Consolas"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding MissingLabel}" FontSize="10"
                                               Foreground="Gray"/>
                                </StackPanel>
                                <Border Grid.Column="2"
                                        Background="{Binding MatchBrush}"
                                        CornerRadius="3" Padding="6,2"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding MatchLabel}"
                                               Foreground="White" FontWeight="SemiBold"
                                               FontSize="11"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding SummaryText}"
                   Foreground="Gray" FontSize="11" Margin="0,4,0,0"/>
    </Grid>
</UserControl>
