# ============================================
# .NET / Visual Studio
# ============================================
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/

# ============================================
# Visual Studio Cache & temp files
# ============================================
*.tlog
*.dbmdl
*.dbproj.schemaview
*.jfm
.vshistory/
StyleCop.Cache

# ============================================
# Rider / VS Code
# ============================================
.idea/
.vscode/
*.DotSettings.user

# ============================================
# NuGet
# ============================================
packages/
*.nupkg
*.snupkg
.nuget/

# ============================================
# Local config / Secrets / User-Data
# ============================================
appsettings.Development.json
appsettings.Local.json
secrets.json
.env
.env.local

# ============================================
# Claude Code & AI-Tooling (interne Entwicklungs-Workflows)
# ============================================
# Kompletter .claude-State raus - enthaelt Session-Daten, Memory-Files
# und projektinterne Settings die nichts im Public-Repo zu suchen haben.
.claude/
.claude/**

# CLAUDE.md ist die interne Architektur-/UX-Iterations-Doku. Sehr
# nuetzlich fuer die Entwicklung, aber:
# - sehr detaillierte Implementierungs-Hinweise (Overload fuer Besucher)
# - Workflow-Historie (UX X.4 .. X.20 etc.) interessiert nur uns
# - kann historische Zwischenstaende enthalten die irrefuehrend wirken
# Public-Variante (Architektur-Auszug) kann spaeter als ARCHITECTURE.md
# nachgereicht werden.
CLAUDE.md

# Interne Release-Prozess-Doku (Tag-Strategie, Velopack-Konstrenten,
# Recovery-Schritte). Bleibt lokal - nicht oeffentlich.
RELEASE_PROCESS.md

# Andere AI-Tooling-Files (falls jemand mit Cursor/Aider/Copilot arbeitet).
PROMPT_*.md
.cursor/
.cursorrules
.aider*
.github/copilot-instructions.md

# ============================================
# Rebrickable-Seed-Daten in PROMPT 6 (2026-05-02) entfernt; Eintraege bleiben
# als Schutz gegen versehentliches Re-Adden, falls jemand die alten ZIPs
# zurueckkopiert.
# ============================================
seed-data/*.zip
HBSort.Core/Resources/CatalogSeed/*.zip

# BrickStore-Datenbank-Download-Cache (~570 MB, wird zur Laufzeit
# heruntergeladen via Settings -> BL-Catalog-Daten -> "Von GitHub importieren")
downloads/

# Lokale Resources (Icons, Screenshots etc. die nicht ins Repo gehoeren)
Resources/
# Hilfe-Inhalte gehoeren explizit ins Repo (Markdown + index.json + Bilder),
# damit sie versioniert sind und in CI/CD-Builds eingebettet werden koennen.
# Reihenfolge wichtig: erst HBSort/Resources/ wieder zugreifbar machen,
# dann den direkten Inhalt erneut sperren (logo.png, app.ico etc. bleiben
# lokal), und schliesslich nur den Help-Unterordner durchlassen.
!/HBSort/Resources/
/HBSort/Resources/*
!/HBSort/Resources/Help/
!/HBSort/Resources/Help/**
# UX X.20+: app.ico (ApplicationIcon) und logo.png (Header) sind essentiell
# fuer den Build und das App-Branding - die werden ins Repo aufgenommen.
# Andere lokale Resources (Screenshots, Mockups etc.) bleiben weiter excluded.
!/HBSort/Resources/app.ico
!/HBSort/Resources/logo.png

# ============================================
# Lokale App-Daten (KRITISCH - duerfen nie ins Repo)
# ============================================
# SQLite-Datenbanken (userdata.db, bl_cache.db, etc.) - enthalten
# persoenliche Sortier-Daten und API-Cache. Auch Sidecar-Files (-shm/-wal).
*.db
*.db-shm
*.db-wal

# settings.json enthaelt DPAPI-verschluesselte BL-Tokens. Auch wenn
# der Inhalt kryptografisch geschuetzt ist: gehoert nicht ins Repo.
settings.json

# User-Bilder + Scan-Snapshots, falls sie im Projekt-Ordner landen
# (Default-Pfad ist AppData, aber defensiv ausschliessen).
images/
scans/

# Migrations-Marker aus dem LegoMinifigSorter-Rename-Schritt.
MIGRATED_TO_HBSort.txt

# ============================================
# Publish-Output (Release-Builds)
# ============================================
# dotnet publish landet hier (Self-Contained-Builds, mehrere 100 MB).
publish/

# ============================================
# Interne Berichte (nicht fuer Public-Repo)
# ============================================
CODE_AUDIT_REPORT.md
CODE_CHECK_REPORT.md
HBSort_Test_Checkliste.pdf
*.pdf

# ============================================
# Logs
# ============================================
*.log
logs/

# ============================================
# Test-Outputs
# ============================================
TestResults/
coverage/
*.trx
*.coverage
*.coveragexml

# ============================================
# Temporary / Backup
# ============================================
*.bak
*.tmp
*~
~*
*.swp

# ============================================
# Diagnose-Files (UX X.33 v0.1.19-beta.7):
# Praxis-Test-Anleitungen die einmalig waehrend einer Bug-Diagnose
# geschrieben werden und danach geloescht werden sollen, aber nicht
# ins Repo gehoeren.
# ============================================
*_DIAGNOSE_*.md
*_PRAXIS_TEST.md
*_TESTANLEITUNG.md
.release-notes-*.md

# ============================================
# Lokale Diagnose-Tools (UX X.33 v0.1.19 Audit):
# Eigene Hilfs-Projekte die nur lokal zum Inspizieren der DB / Logs
# benutzt werden (z.B. .dbinspect aus Block O.1.1). Nicht produktiv.
# ============================================
.dbinspect/

# ============================================
# OS
# ============================================
Thumbs.db
.DS_Store
desktop.ini